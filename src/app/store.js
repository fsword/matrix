/**
 * @class MX.app.Store
 */
MX.kindle('klass', function(X, Klass) {
    "use strict";
    
    X.app.Store = Klass.define({
        alias: 'store',
        
        extend: 'utility'
        
    });
});