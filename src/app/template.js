/**
 * @class MX.app.Template
 */
MX.kindle('klass', function(X, Klass) {
    "use strict";
    
    X.app.Template = Klass.define({
        alias: 'template',
        
        extend: 'utility'
        
    });
});